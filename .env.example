# Local dev (drizzle-kit remote access — never commit real values)
CLOUDFLARE_ACCOUNT_ID=
CLOUDFLARE_DATABASE_ID=
CLOUDFLARE_D1_TOKEN=

# Better Auth (local). Production values go via `wrangler secret put`.
BETTER_AUTH_SECRET=local-dev-secret-change-me-min-32-chars-123456
BETTER_AUTH_URL=http://localhost:3000

# OAuth (local). Production via wrangler secrets.
GOOGLE_CLIENT_ID=
GOOGLE_CLIENT_SECRET=
